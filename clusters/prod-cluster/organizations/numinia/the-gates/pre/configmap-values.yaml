apiVersion: v1
kind: ConfigMap
metadata:
  name: the-gates-pre-values
  namespace: numinia-pre
  annotations:
    config-version: "1.0.0"
data:
  values.yaml: |
    awsSecretName: "hyperfy2-numinia-the-gates-pre"
    orgName: "numinia"
    appName: "the-gates"
    environment: "pre"
    namespace: numinia-pre
    serviceAccountName: "numinia-pre"
    image:
      repository: "ghcr.io/numengames/numinia-hyperfy2"
      tag: "dev"
    world: "pre-the-gates"
    publicMaxUploadSize: "2000"
    publicWsUrl: "https://pre.the-gates.numinia.xyz/ws"
    publicApiUrl: "https://pre.the-gates.numinia.xyz/api"
    assetsBaseUrl: "https://statics.numinia.xyz/hyperfy-spaces/numinia/the-gates/dev/assets"
    clean: "true"
    tlsSecretName: "numinia-tls"
    resources:
      requests:
        cpu: "256m"
        memory: "256Mi"
      limits:
        cpu: "512m"
        memory: "512Mi"
    autoscaling:
      minScale: 0
      maxScale: 1
    domainMapping:
      enabled: true
      hostname: pre.the-gates.numinia.xyz
      namespace: numinia-pre