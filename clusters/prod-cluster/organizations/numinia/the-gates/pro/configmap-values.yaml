apiVersion: v1
kind: ConfigMap
metadata:
  name: the-gates-pro-values
  namespace: numinia
  annotations:
    config-version: "1.0.0"
data:
  values.yaml: |
    awsSecretName: "hyperfy2-numinia-the-gates-pro"
    orgName: "numinia"
    appName: "the-gates"
    environment: "pro"
    namespace: numinia
    serviceAccountName: "numinia"
    image:
      repository: "ghcr.io/numengames/numinia-hyperfy2"
      tag: "dev"
    world: "pro-the-gates"
    publicMaxUploadSize: "1"
    publicWsUrl: "wss://the-gates.numinia.xyz/ws"
    publicApiUrl: "https://the-gates.numinia.xyz/api"
    assetsBaseUrl: "https://statics.numinia.xyz/hyperfy-spaces/numinia/the-gates/latest/assets"
    clean: "true"
    tlsSecretName: "numinia-tls"
    resources:
      requests:
        cpu: "256m"
        memory: "256Mi"
      limits:
        cpu: "512m"
        memory: "512Mi"
    autoscaling:
      minScale: 0
      maxScale: 1
    domainMapping:
      enabled: true
      hostname: the-gates.numinia.xyz
      namespace: numinia