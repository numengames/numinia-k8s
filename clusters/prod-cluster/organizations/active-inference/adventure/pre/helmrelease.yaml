apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: adventure-pre
  namespace: active-inference-pre
spec:
  interval: 5m
  releaseName: adventure-pre
  targetNamespace: active-inference-pre
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./clusters/prod-cluster/base/hyperfy2-knative
      version: 0.3.0
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: adventure-pre-values
      valuesKey: values.yaml