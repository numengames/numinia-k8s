apiVersion: v1
kind: ServiceAccount
metadata:
  name: aws-load-balancer-controller
  namespace: kube-system
  annotations:
    # IRSA role created by numinia-terragrunt (iam/roles/aws-load-balancer-controller).
    # Account-specific ARN: update the account id for a new account (the role NAME stays the same).
    eks.amazonaws.com/role-arn: arn:aws:iam::241533135482:role/numinia-production-eks-aws-load-balancer-controller
