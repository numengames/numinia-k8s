apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: historical-society-pre
  namespace: numinia-pre
spec:
  interval: 5m
  releaseName: historical-society-pre
  targetNamespace: numinia-pre
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./clusters/prod-cluster/base/hyperfy2-knative
      version: 0.3.1
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: historical-society-pre-values
      valuesKey: values.yaml