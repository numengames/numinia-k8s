apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: world-pro
  namespace: r3s3t
spec:
  interval: 5m
  releaseName: world-pro
  targetNamespace: r3s3t
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./clusters/prod-cluster/base/hyperfy2-knative
      sourceRef:
        kind: GitRepository
        name: flux-system-v0.4.4
        namespace: flux-system
      interval: 1m
      version: "0.4.4"
  valuesFrom:
    - kind: ConfigMap
      name: world-r3s3t-pro-values
      valuesKey: values.yaml